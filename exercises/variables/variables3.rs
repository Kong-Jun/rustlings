// variables3.rs
// Make me compile! Execute the command `rustlings hint variables3` if you want a hint :)


fn main() {
    // x 是只读的，不可以被修改
    let x = 3;
    println!("Number {}", x);
    // x = 5;
    println!("Number {}", x);
}
